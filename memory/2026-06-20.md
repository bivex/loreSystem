# Daily Log: 2026-06-20

## Today's Achievements
1. **Resumed Session & Verified Infrastructure**:
   - Confirmed `lore-qdrant` vector database is running on port 6333, and the collection `camel_bridge_memory` is empty (clean state).
   - Confirmed graph explorer and tree servers are running on ports 8080 and 8765, serving `lore_system.db`.
   - Verified that `lore_system.db` has initialized tables and is ready for data generation.
2. **Fixed SQLite Schema and Repository Mismatches**:
   - Detected `IndexError` on character resolution: the `characters` table in `lore_system.db` was initialized with the shorter application schema, which lacked columns expected by `CamelBridgeCharacterRepository` (like `status`, `abilities`, `role`, etc.).
   - Implemented dynamic column check/addition using `ALTER TABLE` in `CamelBridgeCharacterRepository._ensure_schema` and `CamelBridgeEventRepository._ensure_schema` to support unified schemas automatically.
   - Updated core database schemas in `src/infrastructure/sqlite/database.py` for `characters` and `events` to contain all the unified fields.
   - Resolved `sqlite3.IntegrityError: NOT NULL constraint failed: quest_prerequisites.name` by adding a fallback naming strategy in `_GenericBridgeRepository._payload_for`. Entities without explicit names (like `QuestPrerequisite` and `QuestObjective`) will now safely fallback to their label/description or class name.
   - Resolved `sqlite3.IntegrityError: NOT NULL constraint failed: affinities.world_id` by implementing a fallback `world_id` strategy in `_GenericBridgeRepository._payload_for` which loads `CAMEL_BRIDGE_WORLD_ID` from the environment or defaults to `1` when the domain entity lacks a direct `world_id` field (e.g., `Affinity`, `Disposition`).
3. **Fixed Campaign/Story Schema Drift**:
   - Encountered `IndexError: No item with that key` on `story_type` when merging existing `stories`.
   - Added a generic `_ensure_columns` helper method to `_BridgeSQLiteRepository` in `src/infrastructure/camel_bridge_rumor_repository.py`.
   - Updated `_GenericBridgeRepository._ensure_schema` (affecting all generic tables like `quest_prerequisites`, `items` etc.) to execute dynamic `ALTER TABLE` additions for `world_id`, `label`, `payload_json`, `created_at`, `updated_at`, and `version`.
   - Updated `_ensure_schema` across all campaign/story repositories (`CamelBridgeCampaignRepository`, `CamelBridgeStoryRepository`, `CamelBridgeActRepository`, `CamelBridgeChapterRepository`, `CamelBridgeEpisodeRepository`, `CamelBridgePrologueRepository`, `CamelBridgeEpilogueRepository`) to dynamically add missing columns, resolving database crashes when running against application-initialized tables.
4. **Successful Pipeline Execution (Chapter 1)**:
   - Reset the SQLite database tables and wiped/recreated the Qdrant `camel_bridge_memory` collection.
   - Ran pytest to verify all 716 tests pass seamlessly.
   - Executed the full two-pass campaign narrative generation pipeline targeting `lore_system.db` with `output_language=ru`.
   - Generated and persisted 27 documents into Qdrant memory and successfully inserted corresponding campaigns, stories, acts, chapters, episodes, quests, objectives, items, crafting recipes, and progression mechanics into the SQLite database.
5. **Generated Chapter 2**:
   - Ran the pipeline for the second chapter: "Глава 2: Побег из пещеры орков".
   - Passed "Мара Восс" and "Ивен Хейл" as focus characters to ensure narrative continuity.
   - Used Qdrant memory to retrieve relevant background details from the first chapter.
   - Generated and persisted new events, plot branches ("Тайный проход к лесу", "Оборона у врат"), choices ("Как поступить с патрулями у врат?"), moral choices ("Что делать с вождем Громаком?"), zones (Port of Gromak, Eldar Forest), and new items/mechanics.
   - Re-indexed Qdrant memory, bringing the total documents to 49.
   - Cleaned up the campaign title string in the database.
6. **Dialogue/Subtitles Integration & Fixes**:
   - Fixed parser syntax bug in `parsers_characters.py` by removing invalid `fallback=` keyword argument from `_first_non_empty_text` call.
   - Updated `_label_for` and `_payload_for` helpers in `src/infrastructure/camel_bridge_extended_narrative_repository.py` to check for `"text"` key so subtitle texts correctly map to the database `name` column.
   - Imported and registered `CamelBridgeSubtitleRepository` in `test_camel_bridge_rumor_pipeline.py`.
   - Added subtitles mock payload data and added assertions for subtitles count both on result drafts and in database queries in `test_camel_bridge_generates_campaign_story_structure`.
   - Re-ran and verified all 716 tests passing, validating the complete end-to-end flow.
   - Integrated the `subtitles` table as the 5th source in the unified "🗣️ Диалоги и реплики" graph inside `scripts/viewer/database.py`.
   - Subtitle nodes render as lavender rectangles with text previews, language, timing, and are linked to their speaking characters via `произносит` edges.
   - Proactively restarted the Graph Explorer server (killing PID 64168 and launching `run_viewer.py` on port 8080 in the background) to apply the graph changes.
7. **Strict Mode Removal**:
   - Completely removed `--strict-model` parameter from CLI parser (`runner_args.py`), pipeline runner (`run_rumor_pipeline.py`), and runner services (`runner_service.py`).
   - Enforced `allow_fallback=True` directly in the service creation to guarantee resilience against minor LLM JSON formatting glitches.
   - Completely removed the `allow_fallback` parameter from `RumorBridgeService` signature, enforcing fallback mechanism universally.
   - Cleaned up obsolete strict mode tests (like `test_strict_mode_disables_rumor_fallbacks`, `test_strict_mode_disables_chain_fallbacks`, etc.) in `test_camel_bridge_rumor_pipeline.py`.
   - Updated documentation (`README.md`, `COLD.md`, `LORE_GENERATION_PIPELINE.md`) to reflect that strict mode is removed and fallback generation is the standard.
   - Executed the pipeline runner successfully to verify (generated a new subtitle node `sub_4`, bringing total subtitles in `lore_system.db` to 4).
   - Re-ran and confirmed all 712 unit and integration tests pass successfully.
